//! The one binary at the top of the workspace.

fn main() {
    // A stub. What an operator runs and what it prints is #88, and the run that
    // reports the toolchain versions is the second half of #1. Neither is here.
    println!("messlatte {}", env!("CARGO_PKG_VERSION"));
}
