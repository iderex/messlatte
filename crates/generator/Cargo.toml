[package]
name = "messlatte-generator"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
messlatte-units = { path = "../units" }
messlatte-formats = { path = "../formats" }
